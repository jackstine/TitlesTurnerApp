using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using TitlesDatabase.Models;
using TitlesDatabase.Repos;
using TitlesDatabase.Repos.Compositions;

namespace TitlesDatabase
{
    class Program
    {
        static void Main(string[] args)
        {
            //TitlesOtherNamesRepo repo = new TitlesOtherNamesRepo();
            //TitleRepo tRepo = new TitleRepo();
            //int id = 610;
            //foreach(var a in tc.awards)
            //{
            //    Console.Write(a.AwardCompany);
            //}


            //ParticipantRepo pRepo = new ParticipantRepo();
            //var pQuery = pRepo.Get().Where(p => p.Id == 7922997);
            //var participant = pQuery.SelectMany(p => p.TitleParticipants).Select(tp => tp.Title).ToList();
            //Console.ReadLine();
        }
    }
}
